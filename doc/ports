Ports
-----

This document will list everything I discover about what ports are used by
skotos.

Absolute
--------

Baseport Relative
-----------------

BP + 17		UserAPI::Broadcast
BP + 70		server UserDB <- client AuthD
BP + 71		server CtlD <- client CtlD
BP + 79		UserDB billing?
BP + 80		HTTP
BP + 89		Used by DevSys::initd
BP + 90		WOE
BP + 91		DevSys ExportD
BP + 98		Used by System::DevUserD
		first telnet port in config
BP + 99		kernel library admin port
		first binary port in config
BP + 443	Telnet/text login
