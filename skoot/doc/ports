Ports
-----

This document will list everything I discover about what ports are used by
skotos.

Absolute
--------

Baseport Relative
-----------------
Port		Listener, Connector
BP + 17		UserAPI::Broadcast
BP + 70		server UserDB <- client AuthD
BP + 71		server CtlD <- client CtlD
BP + 79		UserDB billing?
BP + 80		HTTP, remote player
BP + 89		DevSys::initd
BP + 90		WOE, remote player
BP + 91		AVChat
BP + 98		System::DevUserD, administrator
BP + 99		kernel library, administrator
BP + 443	TextIF (text connections), remote player
