Actions
-------

The following actions are called by LPC:

language/encounter

	Used when a witness observes speech that is not in the default
	language

login

	Called by TextIF when a user object connects
