# include <config.h>

# define SYSDIR			(USR + "/System")

# define INSTANCE_FILE		(SYSDIR + "/data/instance")
# define USERDB_FILE		(SYSDIR + "/data/userdb")

# define SYS_INITD		(SYSDIR + "/initd")
# define SYS_BOOT		(SYSDIR + "/boot")

# define NTPD			(SYSDIR + "/sys/ntpd")

# define SYSLOGD		(SYSDIR + "/sys/syslogd")

# define MODULED		(SYSDIR + "/sys/moduled")

# define DEV_USERD		(SYSDIR + "/sys/devuserd")
# define DEV_USER		(SYSDIR + "/obj/devuser")
# define DEV_PORT		(SYSDIR + "/obj/devport")

# define OUTBOUND		(SYSDIR + "/obj/outbound")
# define CONFIGD		(SYSDIR + "/sys/configd")

# define PROGDB			(SYSDIR + "/sys/progdb")
# define DBNODE			(SYSDIR + "/obj/dbnode")

# define IDD			(SYSDIR + "/sys/idd")

# define CLONABLE		(SYSDIR + "/lib/clonable")
# define SYS_AUTO		(SYSDIR + "/lib/sys_auto")

# define SYS_LIB_PORT		(SYSDIR + "/lib/port")
# define SYS_LIB_WIZTOOL	(SYSDIR + "/lib/wiztool")

# define SYS_OBJ_WIZTOOL	(SYSDIR + "/obj/wiztool")
# define SYS_LIB_WIZTOOL	(SYSDIR + "/lib/wiztool")

# define SYS_TEXTDATA		(SYSDIR + "/sys/textdata")
# define SYS_TD_ESCAPE		(SYSDIR + "/sys/td-escape")
# define SYS_TD_UNESCAPE	(SYSDIR + "/sys/td-unescape")

# define SYS_URL_ENCODE		(SYSDIR + "/sys/url_encode")
# define SYS_URL_DECODE		(SYSDIR + "/sys/url_decode")

# define TLSD			(SYSDIR + "/sys/tlsd")
# define TOUCHD			(SYSDIR + "/sys/touchd")
